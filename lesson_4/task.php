<?php

/**
 * @param array $bag
 * @param $itNum
 * @return int|null key of snake tail
 */
function getSnakeTail(array $bag, &$itNum) : ?int
{
    $startIndex = 0;
    $endIndex = count($bag) - 1;

    $tailKey = null;

    // TODO: create sorting

    return $tailKey;
};
